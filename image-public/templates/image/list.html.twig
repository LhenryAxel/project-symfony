{% extends 'base.html.twig' %}

{% block body %}
    <h1>Images disponibles</h1>

    <ul>
        {% for image in images %}
            <li>
                <img src="{{ image.url }}" alt="Image" width="200" />
                <p>{{ image.filename }}</p>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
